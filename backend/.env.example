APP_NAME=SentinelAI

# SQLite (default, no setup required)
DATABASE_URL=sqlite:///./sentinelai.db

# PostgreSQL (uncomment and set your password when using)
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/sentinel

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_JWT_SECRET=

# CORS origins for frontend apps (comma-separated)
CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional origin regex
CORS_ALLOW_ORIGIN_REGEX=https?://(localhost|127\.0\.0\.1)(:\d+)?|https://.*\.vercel\.app
